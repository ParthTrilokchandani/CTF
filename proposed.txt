Create a professional client-facing CTF proposal as a Microsoft Word (.docx) document. This is only a proposal explaining the CTF concept and complete player flow — do NOT include installation instructions, scripts, source code, configuration commands, passwords, actual flags, exact exploitation commands, or implementation details.

The CTF will be hosted in a controlled Proxmox virtual environment. Kali Linux / Parrot Security VMs will be the participant/attacker machines using an example 10.10.x.x network, and a dedicated Ubuntu Server will be the CTF target using an example 10.20.x.x network. Both IP ranges are configurable/changeable according to the client's environment. Include a simple professional architecture diagram showing Proxmox → Kali/Parrot participant VMs → Ubuntu CTF target.

Explain the CTF as a realistic, progressive cybersecurity challenge with 5 flags and an intelligence/security-themed storyline. The intended flow is: participants receive the Ubuntu target IP and perform reconnaissance → discover the website → investigate robots.txt → find a hidden webpage and discover encoded Flag 1 after exploring/scrolling the page → investigate an image on the website containing hidden data through steganography → crack the steganography password using a wordlist → extract a text file containing an encoded SSH credential → decode it and gain SSH access as low-privileged Agent99 → investigate Agent99's home directory and discover encoded Flag 2 plus a hidden directory → follow clues toward a backup location → discover encoded Flag 3 and an SSH private key belonging to Agent999 → use the key to access Agent999 → investigate Agent999's shell history and discover encoded Flag 4 → discover a controlled privilege-escalation path involving a hidden .beroot binary → transfer and reverse-engineer the binary using a tool such as Ghidra → recover the required password → complete the controlled privilege escalation to root → find the final encoded Flag 5 inside the root user's home directory.

Mention that Agent99 is intentionally low privileged, Agent99 and Agent999 cannot access each other's protected home directories, and the privilege escalation is intentionally controlled rather than unrestricted.

Include exactly three intentional rabbit holes that increase investigation difficulty without providing shortcuts to the flags or root access. Include a riddle-based hint system where hints guide players without directly revealing solutions.

The proposal should explain each stage at a client/management level, including its purpose, participant objective, expected skill being tested, and how it leads to the next stage. Do not reveal the actual solution details.

Include sections for: Executive Summary, CTF Objectives, Proposed Environment, Architecture, CTF Storyline, Complete Attack/Challenge Flow, Five-Flag Progression, Rabbit Holes, Hint/Riddle System, Skills Tested, Difficulty Progression, Participant Experience, Security/Isolation Considerations, Proposed Deliverables, and Conclusion.

Make the Word document visually professional and suitable for presenting directly to a cybersecurity client. Use clean formatting, tables, flow diagrams, headers/footers, page numbers, and a professional cybersecurity-themed design. State clearly that 10.10.x.x and 10.20.x.x are illustrative, configurable network ranges.

Create the actual .docx file in the current workspace and make sure the document contains only the client-facing proposal, not technical deployment instructions.